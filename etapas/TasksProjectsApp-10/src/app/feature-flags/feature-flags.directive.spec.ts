import { FeatureFlagsDirective } from './feature-flags.directive';

describe('FeatureFlagsDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureFlagsDirective();
    expect(directive).toBeTruthy();
  });
});
