import { NamesDirective } from './names.directive';

describe('NamesDirective', () => {
  it('should create an instance', () => {
    const directive = new NamesDirective();
    expect(directive).toBeTruthy();
  });
});
