import { ReplicateDirective } from './replicate.directive';

describe('ReplicateDirective', () => {
  it('should create an instance', () => {
    /* const directive = new ReplicateDirective();
    expect(directive).toBeTruthy(); */
  });
});
