import { FormatFrameDirective } from './format-frame.directive';

describe('FormatFrameDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatFrameDirective();
    expect(directive).toBeTruthy();
  });
});
