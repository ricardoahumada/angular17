import { FiltraDatosPipe } from './filtra-datos.pipe';

describe('FiltraDatosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraDatosPipe();
    expect(pipe).toBeTruthy();
  });
});
